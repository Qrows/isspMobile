<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ipss.worldbank">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_world"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".country.ScrollViewCountryActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".topic.ScrollViewTopicActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".indicator.ScrollViewIndicatorActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".chart.LoadChartActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".chart.mpchart.ShowChartActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar"></activity>
        <activity android:name=".ChartActivity"></activity>
    </application>

</manifest>